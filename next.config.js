/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"images.unsplash.com",
			"images.pexels.com",
			"s1.ticketm.net",
			"maps.ticketmaster.com",
		],
	},
};

module.exports = nextConfig;
